<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/bg_color">

    <!-- Top Bar -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="68dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="20dp"
        android:translationZ="4dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/btnMenu"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="6dp"
            android:focusable="true"
            android:nextFocusUp="@id/btnMenu"
            android:nextFocusLeft="@id/btnMenu"
            android:nextFocusRight="@id/tabMine"
            android:background="@drawable/bg_view_selector"
            android:src="@drawable/ic_menu"
            app:tint="@color/white" />

        <LinearLayout
            android:id="@+id/tabLayout"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginStart="16dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">
            
            <TextView
                android:id="@+id/tabMine"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="16dp"
                android:paddingVertical="8dp"
                android:text="Mine"
                android:textColor="@color/text_secondary"
                android:textSize="18sp"
                android:clickable="true"
                android:focusable="true"
                android:nextFocusUp="@id/tabMine"
                android:nextFocusLeft="@id/btnMenu"
                android:nextFocusRight="@id/tabDiscovery"
                android:background="@drawable/bg_view_selector" />

            <TextView
                android:id="@+id/tabDiscovery"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="16dp"
                android:paddingVertical="8dp"
                android:text="Discovery"
                android:textColor="@color/text_primary"
                android:textSize="18sp"
                android:textStyle="bold"
                android:clickable="true"
                android:focusable="true"
                android:nextFocusUp="@id/tabDiscovery"
                android:nextFocusLeft="@id/tabMine"
                android:nextFocusRight="@id/tabFindGame"
                android:background="@drawable/bg_view_selector" />

            <TextView
                android:id="@+id/tabFindGame"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="16dp"
                android:paddingVertical="8dp"
                android:text="Find game"
                android:textColor="@color/text_secondary"
                android:textSize="18sp"
                android:clickable="true"
                android:focusable="true"
                android:nextFocusUp="@id/tabFindGame"
                android:nextFocusLeft="@id/tabDiscovery"
                android:nextFocusRight="@id/ivStatusSearch"
                android:background="@drawable/bg_view_selector" />
        </LinearLayout>

        <Space
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1" />

        <!-- Separate Status Icons for high-fidelity UI -->
        <include
            android:id="@+id/statusIcons"
            layout="@layout/layout_status_icons" />

    </LinearLayout>

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <include
        android:id="@+id/sideMenu"
        layout="@layout/layout_side_menu" />

</androidx.drawerlayout.widget.DrawerLayout>
