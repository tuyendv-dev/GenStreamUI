<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GenStreamUI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <!-- Nền cửa sổ + nền splash màu tối để không bị flash trắng khi mở app. -->
        <item name="android:windowBackground">@color/bg_color</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">@color/bg_color</item>
    </style>
</resources>